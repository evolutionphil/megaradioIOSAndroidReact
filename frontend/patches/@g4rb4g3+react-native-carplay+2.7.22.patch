diff --git a/node_modules/@g4rb4g3/react-native-carplay/android/src/main/java/org/birkir/carplay/CarPlayModule.kt b/node_modules/@g4rb4g3/react-native-carplay/android/src/main/java/org/birkir/carplay/CarPlayModule.kt
index 578dbfa..11b93c0 100644
--- a/node_modules/@g4rb4g3/react-native-carplay/android/src/main/java/org/birkir/carplay/CarPlayModule.kt
+++ b/node_modules/@g4rb4g3/react-native-carplay/android/src/main/java/org/birkir/carplay/CarPlayModule.kt
@@ -1,3 +1,4 @@
+// CARPLAY_CONTEXT_FIX_APPLIED
 package org.birkir.carplay
 
 import android.content.Intent
@@ -106,6 +107,10 @@ class CarPlayModule internal constructor(private val reactContext: ReactApplicat
     eventEmitter.didConnect()
   }
 
+  private fun isCarContextReady(): Boolean {
+    return ::carContext.isInitialized
+  }
+
   private fun parseTemplate(
     config: ReadableMap,
     carScreenContext: CarScreenContext
@@ -124,6 +129,14 @@ class CarPlayModule internal constructor(private val reactContext: ReactApplicat
     handler.post {
       Log.d(TAG, "Creating template $templateId")
 
+      if (!isCarContextReady()) {
+        Log.w(TAG, "createTemplate: carContext not initialized yet, skipping template '$templateId'")
+        val args = Arguments.createMap()
+        args.putString("error", "carContext not initialized yet")
+        callback?.invoke(args)
+        return@post
+      }
+
       try {
         createScreen(templateId, config)
         callback?.invoke()
@@ -138,6 +151,10 @@ class CarPlayModule internal constructor(private val reactContext: ReactApplicat
   @ReactMethod
   fun updateTemplate(templateId: String, config: ReadableMap) {
     handler.post {
+      if (!isCarContextReady()) {
+        Log.w(TAG, "updateTemplate: carContext not initialized yet, skipping template '$templateId'")
+        return@post
+      }
       carScreens[templateId]?.let { screen ->
         carScreenContexts[screen]?.let { carScreenContext ->
           val template = parseTemplate(config, carScreenContext)
@@ -163,6 +180,10 @@ class CarPlayModule internal constructor(private val reactContext: ReactApplicat
   fun setRootTemplate(templateId: String, animated: Boolean?) {
     Log.d(TAG, "set Root Template for $templateId")
     handler.post {
+      if (!isCarContextReady()) {
+        Log.w(TAG, "setRootTemplate: carContext not initialized yet, skipping")
+        return@post
+      }
       screenManager?.let {
         val screen = getScreen(templateId)
         if (screen != null) {
@@ -205,6 +226,10 @@ class CarPlayModule internal constructor(private val reactContext: ReactApplicat
   @ReactMethod
   fun pushTemplate(templateId: String, animated: Boolean?) {
     handler.post {
+      if (!isCarContextReady()) {
+        Log.w(TAG, "pushTemplate: carContext not initialized yet, skipping template '$templateId'")
+        return@post
+      }
       val screen = getScreen(templateId)
       if (screen != null) {
         screenManager?.push(screen)
@@ -247,6 +272,10 @@ class CarPlayModule internal constructor(private val reactContext: ReactApplicat
 
   @ReactMethod
   fun toast(text: String, isLongDurationToast: Boolean) {
+    if (!isCarContextReady()) {
+      Log.w(TAG, "toast: carContext not initialized yet, skipping")
+      return
+    }
     val duration = if (isLongDurationToast) CarToast.LENGTH_LONG else CarToast.LENGTH_SHORT
     CarToast.makeText(carContext, text, duration).show()
   }
@@ -254,6 +283,10 @@ class CarPlayModule internal constructor(private val reactContext: ReactApplicat
   @ReactMethod
   fun alert(props: ReadableMap) {
     handler.post {
+      if (!isCarContextReady()) {
+        Log.w(TAG, "alert: carContext not initialized yet, skipping")
+        return@post
+      }
       val id = props.getInt("id")
       val title = Parser.parseCarText(props.getString("title")!!, props)
       val duration = props.getInt("duration").toLong()
@@ -296,6 +329,10 @@ class CarPlayModule internal constructor(private val reactContext: ReactApplicat
 
   @ReactMethod
   fun dismissAlert(alertId: Int) {
+    if (!isCarContextReady()) {
+      Log.w(TAG, "dismissAlert: carContext not initialized yet, skipping")
+      return
+    }
     carContext.getCarService(AppManager::class.java).dismissAlert(alertId)
   }
 
@@ -320,6 +357,11 @@ class CarPlayModule internal constructor(private val reactContext: ReactApplicat
 
   @ReactMethod
   fun getHostInfo(promise: Promise) {
+    if (!isCarContextReady()) {
+      Log.w(TAG, "getHostInfo: carContext not initialized yet")
+      promise.resolve(Arguments.createMap())
+      return
+    }
     return promise.resolve(Arguments.createMap().apply {
       carContext.hostInfo?.packageName?.let { putString("packageName", it) }
       carContext.hostInfo?.uid?.let { putInt("uid", it) }
@@ -415,6 +457,11 @@ class CarPlayModule internal constructor(private val reactContext: ReactApplicat
 
   @ReactMethod
   fun startTelemetryObserver(promise: Promise) {
+    if (!isCarContextReady()) {
+      Log.w(TAG, "startTelemetryObserver: carContext not initialized yet")
+      promise.reject("E_NOT_READY", "carContext not initialized yet")
+      return
+    }
     CarPlayTelemetryObserver.startTelemetryObserver(carContext, eventEmitter, promise)
   }
 
@@ -430,6 +477,11 @@ class CarPlayModule internal constructor(private val reactContext: ReactApplicat
 
   @ReactMethod
   fun requestPermissions(permissions: ReadableArray, message: String, primaryAction: ReadableMap, headerAction: ReadableMap, promise: Promise) {
+    if (!isCarContextReady()) {
+      Log.w(TAG, "requestPermissions: carContext not initialized yet")
+      promise.reject("E_NOT_READY", "carContext not initialized yet")
+      return
+    }
     val list = mutableListOf<String>()
     for (i in 0 until permissions.size()) {
       permissions.getString(i)?.let { list.add(it) }
@@ -441,6 +493,11 @@ class CarPlayModule internal constructor(private val reactContext: ReactApplicat
 
   @ReactMethod
   fun getPlayServicesAvailable(promise: Promise) {
+    if (!isCarContextReady()) {
+      Log.w(TAG, "getPlayServicesAvailable: carContext not initialized yet")
+      promise.resolve(false)
+      return
+    }
     promise.resolve(PlayService.isPlayServiceAvailable(carContext))
   }
 
@@ -464,6 +521,10 @@ class CarPlayModule internal constructor(private val reactContext: ReactApplicat
   }
 
   private fun createScreen(templateId: String, templateConfig: ReadableMap?): CarScreen? {
+    if (!isCarContextReady()) {
+      Log.w(TAG, "createScreen: carContext not initialized yet, skipping")
+      return null
+    }
     if (templateConfig != null) {
       val emitter = EventEmitter(reactContext, templateId)
       val screen = CarScreen(carContext, emitter)
diff --git a/node_modules/@g4rb4g3/react-native-carplay/android/src/main/java/org/birkir/carplay/CarPlaySession.kt b/node_modules/@g4rb4g3/react-native-carplay/android/src/main/java/org/birkir/carplay/CarPlaySession.kt
index 034323b..5f3d532 100644
--- a/node_modules/@g4rb4g3/react-native-carplay/android/src/main/java/org/birkir/carplay/CarPlaySession.kt
+++ b/node_modules/@g4rb4g3/react-native-carplay/android/src/main/java/org/birkir/carplay/CarPlaySession.kt
@@ -19,7 +19,7 @@ import com.facebook.react.bridge.LifecycleEventListener
 import com.facebook.react.bridge.ReactContext
 import com.facebook.react.bridge.WritableNativeMap
 import com.facebook.react.modules.appregistry.AppRegistry
-import com.facebook.react.uimanager.ReactRootViewTagGenerator
+import java.util.concurrent.atomic.AtomicInteger
 import kotlinx.coroutines.CoroutineScope
 import kotlinx.coroutines.Dispatchers
 import kotlinx.coroutines.launch
@@ -109,7 +109,7 @@ class CarPlaySession(
     try {
       val catalystInstance = reactContext.catalystInstance
       val jsAppModuleName = if (isCluster) "AndroidAutoCluster" else "AndroidAuto"
-      val rootTag = ReactRootViewTagGenerator.getNextRootViewTag()
+      val rootTag = getNextRootViewTag()
 
       val appParams = WritableNativeMap().apply {
         putInt("rootTag", rootTag)
@@ -178,6 +178,8 @@ class CarPlaySession(
 
   companion object {
     const val TAG = "CarPlaySession"
+    private val sNextRootViewTag = AtomicInteger(11)
+    fun getNextRootViewTag(): Int = sNextRootViewTag.addAndGet(10)
   }
 
   override fun onHostDestroy() {
diff --git a/node_modules/@g4rb4g3/react-native-carplay/android/src/main/java/org/birkir/carplay/parser/Parser.kt b/node_modules/@g4rb4g3/react-native-carplay/android/src/main/java/org/birkir/carplay/parser/Parser.kt
index f326cee..ce338fc 100644
--- a/node_modules/@g4rb4g3/react-native-carplay/android/src/main/java/org/birkir/carplay/parser/Parser.kt
+++ b/node_modules/@g4rb4g3/react-native-carplay/android/src/main/java/org/birkir/carplay/parser/Parser.kt
@@ -243,11 +243,11 @@ class Parser(
       }
       if (cue.type == ReadableType.Map) {
         val cueMap = cue.asMap()
-        val text = cueMap.getString("text")
-        val image = parseCarIcon(cueMap.getMap("image")!!, context)
-        val alignment = cueMap.getInt("alignment")
-        val start = cueMap.getInt("start")
-        val end = cueMap.getInt("end")
+        val text = cueMap?.getString("text")
+        val image = parseCarIcon(cueMap?.getMap("image")!!, context)
+        val alignment = cueMap?.getInt("alignment") ?: 0
+        val start = cueMap?.getInt("start") ?: 0
+        val end = cueMap?.getInt("end") ?: 0
 
         return SpannableString(text).apply {
           setSpan(
diff --git a/node_modules/@g4rb4g3/react-native-carplay/android/src/main/java/org/birkir/carplay/parser/RCTTemplate.kt b/node_modules/@g4rb4g3/react-native-carplay/android/src/main/java/org/birkir/carplay/parser/RCTTemplate.kt
index 8c8342e..670e991 100644
--- a/node_modules/@g4rb4g3/react-native-carplay/android/src/main/java/org/birkir/carplay/parser/RCTTemplate.kt
+++ b/node_modules/@g4rb4g3/react-native-carplay/android/src/main/java/org/birkir/carplay/parser/RCTTemplate.kt
@@ -187,7 +187,7 @@ abstract class RCTTemplate(
           }
         }
         if (titleVariants.size() > 1) {
-          setText(titleVariants.getString(1))
+          setText(titleVariants.getString(1) ?: "")
         }
       }
       item.getMap("image")?.let { setImage(Parser.parseCarIcon(it, context)) }
@@ -236,7 +236,7 @@ abstract class RCTTemplate(
     )
     props.getArray("texts")?.let {
       for (i in 0 until it.size()) {
-        builder.addText(it.getString(i))
+        builder.addText(it.getString(i) ?: "")
       }
     }
     props.getMap("image")?.let {
diff --git a/node_modules/@g4rb4g3/react-native-carplay/ios/RNCarPlay.m b/node_modules/@g4rb4g3/react-native-carplay/ios/RNCarPlay.m
index 0172a91..5460b59 100644
--- a/node_modules/@g4rb4g3/react-native-carplay/ios/RNCarPlay.m
+++ b/node_modules/@g4rb4g3/react-native-carplay/ios/RNCarPlay.m
@@ -1,7 +1,11 @@
 #import "RNCarPlay.h"
 #import <React/RCTConvert.h>
 #import <React/RCTRootView.h>
-#import "react_native_carplay/react_native_carplay-Swift.h"
+#if __has_include(<react_native_carplay/react_native_carplay-Swift.h>)
+#import <react_native_carplay/react_native_carplay-Swift.h>
+#else
+#import "react_native_carplay-Swift.h"
+#endif
 
 static NSMutableDictionary<NSNumber *, CPNavigationAlert *> *navigationAlertWrappers;
 
diff --git a/node_modules/@g4rb4g3/react-native-carplay/lib/CarPlay.js b/node_modules/@g4rb4g3/react-native-carplay/lib/CarPlay.js
index cd26791..6a9f521 100644
--- a/node_modules/@g4rb4g3/react-native-carplay/lib/CarPlay.js
+++ b/node_modules/@g4rb4g3/react-native-carplay/lib/CarPlay.js
@@ -37,15 +37,32 @@ class CarPlayInterface {
     onVoiceCommandCallbacks = new Set();
     constructor() {
         (0, CarPlayHeadlessJsTask_1.default)();
+        // MEGARADIO FIX: Debounce didConnect events on Android
+        // Native Android Auto module can fire didConnect hundreds of times in rapid succession
+        // causing JS thread to freeze (white screen + lag)
+        let _lastDidConnectTime = 0;
+        const _CONNECT_DEBOUNCE = 2000; // 2 seconds
         this.emitter.addListener('didConnect', (window) => {
-            console.log('we are connected yes!');
+            const now = Date.now();
+            if (now - _lastDidConnectTime < _CONNECT_DEBOUNCE) {
+                return; // Debounce: skip rapid-fire events
+            }
+            _lastDidConnectTime = now;
+            console.log('[CarPlay] didConnect event processed');
             this.connected = true;
             this.window = window;
             this.onConnectCallbacks.forEach(callback => {
                 callback(window);
             });
         });
+        // MEGARADIO FIX: Also debounce didDisconnect
+        let _lastDidDisconnectTime = 0;
         this.emitter.addListener('didDisconnect', () => {
+            const now = Date.now();
+            if (now - _lastDidDisconnectTime < _CONNECT_DEBOUNCE) {
+                return;
+            }
+            _lastDidDisconnectTime = now;
             this.connected = false;
             this.window = undefined;
             this.onDisconnectCallbacks.forEach(callback => {
diff --git a/node_modules/@g4rb4g3/react-native-carplay/lib/src/CarPlay.js b/node_modules/@g4rb4g3/react-native-carplay/lib/src/CarPlay.js
index cd26791..28014f0 100644
--- a/node_modules/@g4rb4g3/react-native-carplay/lib/src/CarPlay.js
+++ b/node_modules/@g4rb4g3/react-native-carplay/lib/src/CarPlay.js
@@ -37,15 +37,29 @@ class CarPlayInterface {
     onVoiceCommandCallbacks = new Set();
     constructor() {
         (0, CarPlayHeadlessJsTask_1.default)();
+        // MEGARADIO FIX: Debounce didConnect events on Android
+        let _lastDidConnectTime = 0;
+        const _CONNECT_DEBOUNCE = 2000;
         this.emitter.addListener('didConnect', (window) => {
-            console.log('we are connected yes!');
+            const now = Date.now();
+            if (now - _lastDidConnectTime < _CONNECT_DEBOUNCE) {
+                return;
+            }
+            _lastDidConnectTime = now;
+            console.log('[CarPlay] didConnect event processed');
             this.connected = true;
             this.window = window;
             this.onConnectCallbacks.forEach(callback => {
                 callback(window);
             });
         });
+        let _lastDidDisconnectTime = 0;
         this.emitter.addListener('didDisconnect', () => {
+            const now = Date.now();
+            if (now - _lastDidDisconnectTime < _CONNECT_DEBOUNCE) {
+                return;
+            }
+            _lastDidDisconnectTime = now;
             this.connected = false;
             this.window = undefined;
             this.onDisconnectCallbacks.forEach(callback => {
